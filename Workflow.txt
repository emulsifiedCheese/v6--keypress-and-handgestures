1st row
	webcam image -> 160 x 120p
	convert image to greyscale
		increase brightness by 20%
		place beside og webcam image
		prevent pixel intensity past 255

2nd row
	split og webcam image into RGB color channels
	
3rd row
	RGB color channels with seperate threeshold sliders
	explain result of thresholding for each channel

4th row
	og webcam image + HSV + YCbCr
	
5th row
	face detection with hand gesture + HSV + YCbCr
		hand gestures control greyscale (thumb to index), 
		blur (thumb to pinkie, face not recognisable), 
		HSV (index to middle), 
		pixel filter (thumb to middle)
		
		use sliders to control threshold of color spaces
		explain result of thresholding for each color space/is thresholded image noisier, etc
		
Perform face detection as described in the lecture videos. Here you have two options: 
	you could use the approach shown in the course (see Week 19) or the face api from 
	here: https://learn.ml5js.org/#/reference/face-api.

13. Display the detected face at the correct position in the grid - then by using keystrokes of your choice - replace the detected face image with:
	a. A greyscale image (pressing 1)
	b. A blurred image – adjust blurring so your face is not recognisable (pressing 2)
	c. A colour converted image – reuse code from task 9, HSV (pressing 3)
	d. A pixelate image. To perform pixelation use the following approach (pressing 4)
		i. Run step a – so that your image is greyscale
		ii. Split the detected face image into 5x5 pixel blocks
		iii. Calculate the average pixel intensity of each block using image.get(x, 
		y); (to access each pixel’s intensity)
		iv. Paint the entire block using the average pixel intensity. Utilise this 
		command: outimage.set(x, y, avePixInt);
		v. Loop through all blocks repeating steps iii and iv

Label each grid image that matches the keywords in the marking rubrics

add name and student id to the white spaces on the right